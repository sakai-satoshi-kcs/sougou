import 'package:flutter/material.dart';

class ReminderScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('リマインダー')),
      body: Center(child: Text('リマインダー画面')),
    );
  }
}
